<!-- Kivi.App/Views/MainApp/AnalyticsPage.xaml -->
<!-- Real analytics derived entirely from ITranscriptStore (no mock data). Stat tiles,
     a words-over-time bar chart, top-apps breakdown, and dictation-type split all read
     from persisted transcript history. -->
<Page
    x:Class="Kivi.App.Views.MainApp.AnalyticsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Background="{ThemeResource KiviSurfaceAltBrush}">
    <ScrollViewer Padding="32,28,32,28">
        <StackPanel Spacing="20" MaxWidth="920">
            <TextBlock Text="analytics" FontFamily="{ThemeResource KiviFontFamily}"
                       FontSize="{ThemeResource KiviFontSizeTitle}"
                       FontWeight="{ThemeResource KiviFontWeightSemibold}"
                       Foreground="{ThemeResource KiviTextPrimaryBrush}"/>

            <Grid ColumnSpacing="12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/><ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/><ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Border Grid.Column="0" Style="{StaticResource KiviCardStyle}" Padding="18,16">
                    <StackPanel Spacing="6">
                        <TextBlock Text="WORDS" FontFamily="{ThemeResource KiviMonoFontFamily}" FontSize="10.5"
                                   FontWeight="SemiBold" Foreground="{ThemeResource KiviTextTertiaryBrush}"/>
                        <TextBlock x:Name="TotalWordsText" FontSize="26"
                                   FontFamily="{ThemeResource KiviFontFamily}"
                                   FontWeight="{ThemeResource KiviFontWeightSemibold}"
                                   Foreground="{ThemeResource KiviTextPrimaryBrush}"/>
                    </StackPanel>
                </Border>
                <Border Grid.Column="1" Style="{StaticResource KiviCardStyle}" Padding="18,16">
                    <StackPanel Spacing="6">
                        <TextBlock Text="WORDS / MIN" FontFamily="{ThemeResource KiviMonoFontFamily}" FontSize="10.5"
                                   FontWeight="SemiBold" Foreground="{ThemeResource KiviTextTertiaryBrush}"/>
                        <TextBlock x:Name="WordsPerMinText" FontSize="26"
                                   FontFamily="{ThemeResource KiviFontFamily}"
                                   FontWeight="{ThemeResource KiviFontWeightSemibold}"
                                   Foreground="{ThemeResource KiviTextPrimaryBrush}"/>
                    </StackPanel>
                </Border>
                <Border Grid.Column="2" Style="{StaticResource KiviCardStyle}" Padding="18,16">
                    <StackPanel Spacing="6">
                        <TextBlock Text="TIME SPOKEN" FontFamily="{ThemeResource KiviMonoFontFamily}" FontSize="10.5"
                                   FontWeight="SemiBold" Foreground="{ThemeResource KiviTextTertiaryBrush}"/>
                        <TextBlock x:Name="TimeSpokenText" FontSize="26"
                                   FontFamily="{ThemeResource KiviFontFamily}"
                                   FontWeight="{ThemeResource KiviFontWeightSemibold}"
                                   Foreground="{ThemeResource KiviTextPrimaryBrush}"/>
                    </StackPanel>
                </Border>
                <Border Grid.Column="3" Style="{StaticResource KiviCardStyle}" Padding="18,16">
                    <StackPanel Spacing="6">
                        <TextBlock Text="DICTATIONS" FontFamily="{ThemeResource KiviMonoFontFamily}" FontSize="10.5"
                                   FontWeight="SemiBold" Foreground="{ThemeResource KiviTextTertiaryBrush}"/>
                        <TextBlock x:Name="DictationCountText" FontSize="26"
                                   FontFamily="{ThemeResource KiviFontFamily}"
                                   FontWeight="{ThemeResource KiviFontWeightSemibold}"
                                   Foreground="{ThemeResource KiviTextPrimaryBrush}"/>
                    </StackPanel>
                </Border>
            </Grid>

            <Border Style="{StaticResource KiviCardStyle}" Padding="20">
                <StackPanel Spacing="16">
                    <TextBlock Text="WORDS OVER TIME" FontFamily="{ThemeResource KiviMonoFontFamily}" FontSize="10.5"
                               FontWeight="SemiBold" Foreground="{ThemeResource KiviTextTertiaryBrush}"/>
                    <Grid>
                        <StackPanel x:Name="WordsOverTimePanel" Orientation="Horizontal" Spacing="8"
                                    Height="130" VerticalAlignment="Bottom"/>
                        <TextBlock x:Name="WordsOverTimeEmptyText" Text="No dictations yet — your daily word counts will show up here."
                                   FontFamily="{ThemeResource KiviFontFamily}" FontSize="13"
                                   Foreground="{ThemeResource KiviTextTertiaryBrush}"
                                   HorizontalAlignment="Center" VerticalAlignment="Center"
                                   Visibility="Collapsed"/>
                    </Grid>
                </StackPanel>
            </Border>

            <Grid ColumnSpacing="16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Style="{StaticResource KiviCardStyle}" Padding="20">
                    <StackPanel Spacing="14">
                        <TextBlock Text="TOP APPS" FontFamily="{ThemeResource KiviMonoFontFamily}" FontSize="10.5"
                                   FontWeight="SemiBold" Foreground="{ThemeResource KiviTextTertiaryBrush}"/>
                        <StackPanel x:Name="TopAppsPanel" Spacing="10"/>
                        <TextBlock x:Name="TopAppsEmptyText" Text="No app data captured yet."
                                   FontFamily="{ThemeResource KiviFontFamily}" FontSize="13"
                                   Foreground="{ThemeResource KiviTextTertiaryBrush}" Visibility="Collapsed"/>
                    </StackPanel>
                </Border>

                <Border Grid.Column="1" Style="{StaticResource KiviCardStyle}" Padding="20">
                    <StackPanel Spacing="14">
                        <TextBlock Text="DICTATION TYPE" FontFamily="{ThemeResource KiviMonoFontFamily}" FontSize="10.5"
                                   FontWeight="SemiBold" Foreground="{ThemeResource KiviTextTertiaryBrush}"/>
                        <StackPanel x:Name="DictationTypePanel" Spacing="10"/>
                    </StackPanel>
                </Border>
            </Grid>
        </StackPanel>
    </ScrollViewer>
</Page>
