<!-- Kivi.App/Views/MainApp/PresetsPage.xaml -->
<!-- UI-only per the design spec: backed by WorkspaceMockData.Presets (in-memory, resets on
     restart). A simple list of preset name + instruction cards, with add/delete. -->
<Page
    x:Class="Kivi.App.Views.MainApp.PresetsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Background="{ThemeResource KiviSurfaceAltBrush}">
    <ScrollViewer Padding="32,28,32,28">
        <StackPanel Spacing="18" MaxWidth="760">
            <Grid>
                <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="presets" FontFamily="{ThemeResource KiviFontFamily}"
                           FontSize="{ThemeResource KiviFontSizeTitle}"
                           FontWeight="{ThemeResource KiviFontWeightSemibold}"
                           Foreground="{ThemeResource KiviTextPrimaryBrush}"/>
                <Button Grid.Column="1" Click="OnNewPreset"
                        Background="Transparent" BorderBrush="{ThemeResource KiviStrokeBrush}" BorderThickness="1"
                        CornerRadius="{ThemeResource KiviRadiusSm}" Padding="14,8">
                    <TextBlock Text="+ new preset" Foreground="{ThemeResource KiviTextSecondaryBrush}"
                               FontFamily="{ThemeResource KiviFontFamily}" FontSize="13"/>
                </Button>
            </Grid>
            <StackPanel x:Name="PresetsPanel" Spacing="12"/>
        </StackPanel>
    </ScrollViewer>
</Page>
