<!-- Kivi.App/Views/Onboarding/PreferencesPage.xaml -->
<Page
    x:Class="Kivi.App.Views.Onboarding.PreferencesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Background="{ThemeResource KiviSurfaceAltBrush}">
    <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto"
                  HorizontalContentAlignment="Center" Padding="0,40,0,40">
        <StackPanel Width="460" Spacing="16">

            <StackPanel Spacing="4" Margin="4,0,4,4">
                <TextBlock Text="a couple of quick preferences" FontFamily="{ThemeResource KiviFontFamily}"
                           FontSize="{ThemeResource KiviFontSizeTitle}"
                           FontWeight="{ThemeResource KiviFontWeightSemibold}"
                           Foreground="{ThemeResource KiviTextPrimaryBrush}"/>
                <TextBlock Text="helps kivi tune itself to you — change these anytime later"
                           FontFamily="{ThemeResource KiviFontFamily}"
                           FontSize="{ThemeResource KiviFontSizeBody}"
                           Foreground="{ThemeResource KiviTextSecondaryBrush}"/>
            </StackPanel>

            <!-- Language card -->
            <Border Style="{StaticResource KiviCardStyle}">
                <StackPanel Spacing="12">
                    <StackPanel Spacing="2">
                        <TextBlock Text="Which languages do you dictate in?" FontFamily="{ThemeResource KiviFontFamily}"
                                   FontSize="{ThemeResource KiviFontSizeBody}"
                                   FontWeight="{ThemeResource KiviFontWeightMedium}"
                                   Foreground="{ThemeResource KiviTextPrimaryBrush}"/>
                        <TextBlock Text="auto-detects by default" FontFamily="{ThemeResource KiviFontFamily}"
                                   FontSize="{ThemeResource KiviFontSizeCaption}"
                                   Foreground="{ThemeResource KiviTextSecondaryBrush}"/>
                    </StackPanel>
                    <StackPanel x:Name="LanguageChipPanel" Orientation="Horizontal" Spacing="8"/>
                </StackPanel>
            </Border>

            <!-- Primary use case card -->
            <Border Style="{StaticResource KiviCardStyle}">
                <StackPanel Spacing="12">
                    <StackPanel Spacing="2">
                        <TextBlock Text="What do you use typing for most?" FontFamily="{ThemeResource KiviFontFamily}"
                                   FontSize="{ThemeResource KiviFontSizeBody}"
                                   FontWeight="{ThemeResource KiviFontWeightMedium}"
                                   Foreground="{ThemeResource KiviTextPrimaryBrush}"/>
                        <TextBlock Text="for display purposes only" FontFamily="{ThemeResource KiviFontFamily}"
                                   FontSize="{ThemeResource KiviFontSizeCaption}"
                                   Foreground="{ThemeResource KiviTextSecondaryBrush}"/>
                    </StackPanel>
                    <StackPanel x:Name="UseCasePanel" Orientation="Vertical" Spacing="8"/>
                </StackPanel>
            </Border>

            <Button x:Name="ContinueButton" Content="Continue" Click="OnContinue"
                    HorizontalAlignment="Stretch" Margin="0,8,0,0"
                    Style="{StaticResource KiviPrimaryButtonStyle}"/>
        </StackPanel>
    </ScrollViewer>
</Page>
